<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/chosenBook.css">


</head>
<body>

<div class="header">
    <div class="title"> {{BOOK.title}}</div>
</div>

<div class="main">
    <div class="grid-container">
        <div class="book-author grid-item"> {{BOOK.author}}</div>
        <div class="book-quotes grid-item">
            {{#QUOTES}}
                {{quote}}
            {{/QUOTES}}
        </div>
        <div class="book-pages grid-item"> {{BOOK.pageCurrent}}/{{BOOK.pagesTotal}}</div>
        <div class="book-cover grid-item">
            <img src="/{{BOOK.booKUrl}}" alt="">
        </div>
        <div class="book-progress grid-item"> {{BOOK.progress}}%</div>
    </div>
</div>

<nav class="menu-bar">
    <form method="get" action="/dashboard">
        <button class="go-dash-button" type="submit">Bookshelf</button>
    </form>
    <form method="get" action="/quotes">
        <button class="go-quotes-button" type="submit">Quotes</button>
    </form>
    <form method="get" action="/impressum">
        <button class="go-impressum-button" type="submit">Impressum</button>
    </form>
</nav>



</body>
</html>